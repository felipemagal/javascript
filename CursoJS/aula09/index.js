// TIPO DE DADOS PRIMITIVOS - string, number, null, boolean, undefined e symbol

const nome = "Felipe"; // string
const nome1 = "Felipe"; //string
const nome2 = `Felipe`; //string
const num1 = 10; //number
const num2 = 10.52; //number
let nomeAluno; //undefined -> não aponta para local nenhum na memória
const sobrenomeAluno = null; //nulo -> não apont para local nenhum na memória
const aprovado = false; //boolean -> true ou false (lógico)

console.log(typeof num1);
